[build-system]
requires = ["hatchling>=1.27.0,<2.0"]
build-backend = "hatchling.build"

[project]
name = "alchitry-au2-level-shifter-py"
version = "0.1.0"
description = "JITX design for an Ft-safe Alchitry Au2 RetroBus level-shifter element"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "alchitry-v2-elements-py",
  "jitx>=4.0.0,<5",
  "jitxlib-parts>=1.0.0,<2",
  "jitxlib-standard>=4.0.0,<5",
  "jitxexamples-components>=1.0.0,<2",
  "shared-components",
]

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project.optional-dependencies]
dev = [
  "ruff",
  "ty",
]

[tool.uv.sources]
alchitry-v2-elements-py = { path = "../alchitry-v2-elements", editable = true }
shared-components = { path = "../shared-components", editable = true }

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.uv]
exclude-newer = "2026-08-31T00:00:00Z"
